{
  "name": "crud-json",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "json-server --watch db.json --port 5000",
    "serve": "json-server --watch db.json --port 5000"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "json": "^11.0.0",
    "nodemon": "^3.1.7",
    "server": "^1.0.41"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
